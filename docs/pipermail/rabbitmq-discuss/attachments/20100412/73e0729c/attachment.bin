diff -r 825eceaca84c src/amqp_rpc_server.erl
--- a/src/amqp_rpc_server.erl	Thu Nov 05 14:17:13 2009 +0000
+++ b/src/amqp_rpc_server.erl	Fri Apr 09 11:20:27 2010 +0100
@@ -89,7 +89,7 @@
     {stop, normal, State};
 
 %% @private
-handle_info({#'basic.deliver'{},
+handle_info({#'basic.deliver'{delivery_tag = DeliveryTag},
              #amqp_msg{props = Props, payload = Payload}},
             State = #rpc_server_state{handler = Fun, channel = Channel}) ->
     #'P_basic'{correlation_id = CorrelationId,
@@ -101,6 +101,7 @@
                                mandatory = true},
     amqp_channel:call(Channel, Publish, #amqp_msg{props = Properties,
                                                   payload = Response}),
+    amqp_channel:cast(Channel, #'basic.ack'{delivery_tag = DeliveryTag}),
     {noreply, State}.
 
 %% @private
