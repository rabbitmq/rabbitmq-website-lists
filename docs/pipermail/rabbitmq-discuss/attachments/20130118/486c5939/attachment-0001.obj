	{ ${RABBIT_QUEUE_NAME}, [
		  {sources,      [{broker, "amqp://${RABBIT_POD_ADMIN}:${RABBIT_POD_PASSWORD}@"}] }
		, {destinations, [{broker, "amqp://${RABBIT_ENTERPRISE_ADMIN}:${RABBIT_ENTERPRISE_ADMIN_PASSWORD}@${RABBIT_ENTERPRISE_HOST}/${RABBIT_ENTERPRISE_V_HOST}"}] }
		, {queue, <<"${RABBIT_QUEUE_NAME}">>}
		, {prefetch_count, 0}
		, {publish_properties, [ 
			{delivery_mode, 2}
			, {headers, [{<<"source">>,longstr,<<"${HOSTNAME}">>}] } 
			]}
		, {reconnect_delay, 5}
	] }
