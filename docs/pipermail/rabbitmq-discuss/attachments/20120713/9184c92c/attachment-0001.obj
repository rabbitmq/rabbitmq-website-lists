import pika
import sys

connection = pika.BlockingConnection(pika.ConnectionParameters(host="localhost"))
channel = connection.channel()

for i in range(100):
    channel.queue_declare(queue='queue_' + str(i),
                          arguments={'x-ha-policy': 'all'},
                          durable=True)

connection.close()
