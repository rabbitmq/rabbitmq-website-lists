Index: rabbit.c
===================================================================
--- rabbit.c	(revision 18860)
+++ rabbit.c	(revision 18861)
@@ -907,8 +907,9 @@
 		if (count > -1) {			
 			char * tmp = estrdup( frame.payload.body_fragment.bytes );
 			uint len = frame.payload.body_fragment.len;
-			add_assoc_stringl( return_value,
+			add_assoc_stringl_ex( return_value,
 			    "msg",
+			    4,
 			    tmp,
 				len,
 				1 );		